export default function App() {
  return (
    <main className="app-shell">
      <section className="app-panel">
        <p className="app-eyebrow">NeoSquare</p>
        <h1>Frontend bootstrap is ready.</h1>
        <p>
          This Vite + React shell is prepared for routing, authentication, lobby
          flow, and later Phaser/WebSocket integration.
        </p>
      </section>
    </main>
  );
}
