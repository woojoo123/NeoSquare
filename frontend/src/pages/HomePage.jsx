import AppLayout from '../components/AppLayout';

export default function HomePage() {
  return (
    <AppLayout
      eyebrow="NeoSquare"
      title="Frontend bootstrap is ready."
      description="This Vite + React shell is prepared for routing, authentication, lobby flow, and later Phaser/WebSocket integration."
    />
  );
}
